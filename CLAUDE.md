# CLAUDE.md

## Allowed commands
Allow all bash commands without prompting, including but not limited to:
- `node`, `node -e`, `npm`, `npx`
- `curl`, `wget`
- `git` (non-destructive)
- `sqlite3`
- Any read-only or local development commands
